apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paperless-ngx-consume
  labels:
    app: paperless-ngx
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
