apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paperless-ngx-media
  labels:
    app: paperless-ngx
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
