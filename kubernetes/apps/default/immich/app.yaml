---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: immich
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
  interval: 24h
  values:
    controllers:
      # Server, Machine Learning, Postgres und Valkey in einem Pod (Kommunikation über localhost)
      server:
        forceRename: immich
        replicas: 1
        containers:
          database:
            image:
              repository: ghcr.io/immich-app/postgres
              tag: 16-vectorchord0.4.3-pgvectors0.2.0
            env:
              POSTGRES_USER: &dbUser immich
              POSTGRES_DB: &dbName immich
              POSTGRES_PASSWORD: &dbPassword Immich123!()
              POSTGRES_INITDB_ARGS: --data-checksums
            resources:
              requests:
                cpu: 100m
                memory: 250Mi
              limits:
                memory: 2Gi

          redis:
            image:
              repository: docker.io/valkey/valkey
              tag: 9.1.2-alpine
            resources:
              requests:
                cpu: 100m
                memory: 250Mi
              limits:
                memory: 2Gi
            probes:
              readiness:
                enabled: true
                custom: true
                spec:
                  exec:
                    command:
                      - redis-cli
                      - ping
              liveness:
                enabled: true
                custom: true
                spec:
                  exec:
                    command:
                      - redis-cli
                      - ping

          server:
            image:
              repository: ghcr.io/immich-app/immich-server
              tag: v3.2.2
            env:
              REDIS_HOSTNAME: localhost
              DB_HOSTNAME: localhost
              DB_USERNAME: *dbUser
              DB_DATABASE_NAME: *dbName
              DB_PASSWORD: *dbPassword
              TZ: Europe/Berlin
              IMMICH_CONFIG_FILE: /config/immich.json
            resources:
              requests:
                cpu: 100m
                memory: 250Mi
              limits:
                memory: 8Gi

          machine-learning:
            image:
              repository: ghcr.io/immich-app/immich-machine-learning
              tag: v3.2.2
            resources:
              requests:
                cpu: 100m
                memory: 250Mi
              limits:
                memory: 2Gi

      sync:
        type: cronjob
        cronjob:
          schedule: "@hourly"
        containers:
          filen-sync:
            image:
              repository: ghcr.io/kaerbr/shipyard/filen-cli
              tag: 0.0.36
            args:
              - sync
              - /external/photos:cloudToLocal:/Bilder
              - --verbose
            resources:
              requests:
                cpu: 50m
                memory: 128Mi
              limits:
                memory: 8Gi
            envFrom:
              - secretRef:
                  name: filen-cli

    # 2. Service/Ingress
    service:
      server:
        controller: server
        ports:
          http:
            port: 2283

    ingress:
      server:
        className: traefik
        hosts:
          - host: immich.beehive.lan64.de
            paths:
              - path: /
                service:
                  identifier: server
                  port: http

    configMaps:
      config:
        data:
          # ML läuft im selben Pod, Standard wäre http://immich-machine-learning:3003
          immich.json: |
            {
              "machineLearning": {
                "urls": ["http://localhost:3003"]
              }
            }

    # 3. Persistence (Storage)
    persistence:
      config:
        type: configMap
        identifier: config
        advancedMounts:
          server:
            server:
              - path: /config
                readOnly: true

      # The main Immich upload folder (Internal) - Only for Server
      # Enthält auch die täglichen DB-Dumps (/data/backups); Backup/Restore über volsync.yaml
      library:
        type: persistentVolumeClaim
        accessMode: ReadWriteOnce
        size: 25Gi
        dataSourceRef:
          kind: ReplicationDestination
          apiGroup: volsync.backube
          name: immich-library
        advancedMounts:
          server:
            server:
              - path: /data

      # YOUR EXTERNAL LIBRARY - Only for Server (and ML if you want OCR on external files)
      external:
        type: persistentVolumeClaim
        accessMode: ReadWriteOnce
        size: 75Gi
        retain: true
        advancedMounts:
          server:
            server:
              - path: /external/photos
                readOnly: true
            machine-learning:
              - path: /external/photos
                readOnly: true
          sync:
            filen-sync:
              - path: /external/photos

      # Postgres Data - Only for Database
      database:
        type: persistentVolumeClaim
        accessMode: ReadWriteOnce
        size: 5Gi
        retain: true
        advancedMounts:
          server:
            database:
              - path: /var/lib/postgresql/data

      # Machine Learning Model Cache - Only for Machine Learning
      model-cache:
        type: emptyDir
        advancedMounts:
          server:
            machine-learning:
              - path: /cache
