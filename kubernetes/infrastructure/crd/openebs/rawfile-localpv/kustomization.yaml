namespace: openebs
resources:
  - namespace.yaml
  - helm.yaml
configMapGenerator:
  - name: rawfile-localpv-helm-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
