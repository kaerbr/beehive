---
machine:
  network:
    hostname: &hostname virtualbox-02
    interfaces:
      - deviceSelector:
          busPath: "0*" # should select any hardware network device, if you have just one, it will be selected
      # Assigns static IP addresses to the interface.
        addresses:
          - &ipv4Address 192.168.178.182/24
    # Specify dns hostname lookups for other nodes in the cluster.
    extraHostEntries:
      - ip: *ipv4Address
        aliases:
          - *hostname

  install:
    disk: /dev/sda
